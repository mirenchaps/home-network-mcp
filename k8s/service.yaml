apiVersion: v1
kind: Service
metadata:
  name: home-network-mcp
  labels:
    app: home-network-mcp
spec:
  type: NodePort
  selector:
    app: home-network-mcp
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
