apiVersion: v1
kind: Service
metadata:
  name: home-network-mcp-server
  labels:
    app: home-network-mcp-server
spec:
  type: NodePort
  selector:
    app: home-network-mcp-server
  ports:
    - port: 8001
      targetPort: 8001
      nodePort: 30081
